<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">
    <div class="container-fluid">
      <h3><%= link_to "Payroll Program", root_path %></h3>
      <nav>
        <ul class="nav pull-right">
          <li id="fat-menu" class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Workers</a>
            <ul class="dropdown-menu">
              <li><%= link_to "Add New Worker", new_worker_path %></li> 
              <li class="divider"></li>
              <% Worker.all.each do |child| %>
                <li><%= link_to child.fname, worker_path(child) %></li>
              <% end %>
            </ul>
          </li>
        </ul>
      </nav>
    </div>
  </div>
</header>
